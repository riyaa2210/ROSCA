{
  "name": "bhishi-app",
  "version": "1.0.0",
  "description": "Bhishi Money Pooling App — monorepo root",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
